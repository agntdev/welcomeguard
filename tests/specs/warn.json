[
  {
    "name": "/warn requires admin permissions",
    "steps": [
      {
        "send": { "text": "/warn" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Only admins can use this command."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "/warn shows usage without target",
    "steps": [
      {
        "send": { "text": "/warn test" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Only admins can use this command."
            }
          }
        ]
      }
    ]
  }
]
