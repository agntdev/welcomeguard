[
  {
    "name": "/setrules requires admin permissions",
    "steps": [
      {
        "send": { "text": "/setrules" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Only admins can change settings."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "/setrules with text requires admin permissions",
    "steps": [
      {
        "send": { "text": "/setrules Be respectful" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Only admins can change settings."
            }
          }
        ]
      }
    ]
  }
]
